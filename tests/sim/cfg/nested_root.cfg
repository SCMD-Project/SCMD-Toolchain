// sim_nested_exec + scmdsim_lazy_cfg_mode + scmdsim_exec_path_completion:
// a root module that execs a nested module (2 lazy compiles total) and a
// nested/ directory for exec-path completion.
echoln ROOT_A
exec nested/child
echoln ROOT_B
